<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>And so it begins...</title>
  </head>
  <body>

    <h1><%="#{@team.name}"%></h1>
    <h2>Team Motto: <%="#{@team.motto}"%></h2>

    <% @team.heroes.each do |hero| %>
      <h3>Hero Name: <%= hero.name %></h3>
      <ul>
        <li>Hero Power: <%= hero.power %>
        <li>Hero Biography: <%= hero.bio %>
      </ul>
    <% end %>

  </body>
</html>
